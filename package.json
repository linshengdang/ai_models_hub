{
  "name": "demoui4ai",
  "version": "1.0.0",
  "description": "SpaceDream AI Model Hub - 现代化多大语言模型集成管理平台与 API 代理网关，支持一键部署，无缝接入 Cursor / VS Code 等开发工具",
  "author": "dawn (SpaceDream)",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install",
    "start": "npm run build && npm run server"
  },
  "dependencies": {
    "concurrently": "^9.0.1",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "mime-types": "^2.1.35",
    "multer": "^1.4.5-lts.1",
    "uuid": "^10.0.0"
  }
}
